import React, { memo } from "react"
import { Text, View } from "react-native"

const Playlists = () => {
  return (
    <View>
      <Text>Playlists</Text>
    </View>
  )
}

export default memo(Playlists)
