import { Track } from "react-native-track-player"
import { Ifavourites } from "../Interfaces/favourite.interfacee"
export default class FavouriteService implements Ifavourites {
    addToFav = (currentSong: Track) => {

    }
}